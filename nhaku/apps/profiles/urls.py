from django.conf.urls import url
from . import views

urlpatterns = [
    # url(
    #     r'^panel/nota-ingreso/$',
    #     views.DepositSlipView.as_view(),
    #     name='nota-ingreso'
    # ),
]
